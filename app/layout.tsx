import type { Metadata } from "next";
import { Inter, Geist_Mono } from "next/font/google";
import "./globals.css";

const inter = Inter({
  variable: "--font-inter",
  subsets: ["latin"],
  display: "swap",
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "COMPETE — Competitive Intelligence Platform",
  description:
    "AI-powered competitive intelligence that discovers, analyzes, and helps you outperform your market rivals. Scrape competitor content, find gaps, and generate on-brand posts.",
  keywords: [
    "competitive intelligence",
    "competitor analysis",
    "content gap analysis",
    "social media monitoring",
    "AI content generation",
  ],
};

export default function RootLayout({ children }: LayoutProps<"/">) {
  return (
    <html
      lang="en"
      className={`${inter.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
